<!DOCTYPE html>
<html>
  <meta charset='utf-8'>
  <head>
    <title>TimeStamp API</title>
  </head>
  <body>
    <h1>TimeStamp MicroService</h1>
    <ol>
      <li>Pass a string as a parameter into the url, and it will check whether it is a Unix timestamp or a natural language date.</li>
      <li>If it is, the API will return both the Unix timestamp and the natural language date.</li>
      <li>If it is not a valid timestamp or date, the API will return null for both properties.</li>
    </ol>
  </body>
</html>
